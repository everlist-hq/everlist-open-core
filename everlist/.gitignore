# runtime state contains booking secrets, admin keys, wallets - NEVER commit
.run/
state.json
*.state.json
# H4 lockfiles + any runtime locks (flock discipline files)
*.lock

# python
venv/
__pycache__/
*.pyc

# local env / secrets
.env
*.key

# canonical agent identity seed (TESTNET ONLY - never commit, never attach real funds)
.secrets/
